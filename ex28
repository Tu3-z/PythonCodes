import random
n = int(input('Digite um número de 1 à 5: '))
np = random.randint(1, 5)
if n == np:
    print('O número escolhido pelo computador também foi {}, parabéns!'.format(n))
else:
    print('O número escolhido pelo computador foi {}, você perdeu!'.format(np))